# מערכת הזמנות ציוד דת — רבנות אוגדה 98

## מבנה הפרויקט

```
/                      ← הריפו הזה, מוגש ע"י GitHub Pages
├── index.html         ← הממשק
├── app.js             ← כניסה, פרופיל, הזמנות
├── admin.js           ← ממשק ניהול
├── lib.js             ← ליבה משותפת (חיבור, מצב, כלי עזר)
├── styles.css
├── config.js          ← כתובת ומפתח Supabase
├── logo.png
└── supabase/
    ├── schema.sql     ← טבלאות, RLS, פונקציות SQL
    ├── migrations/    ← שינויי סכימה (supabase db push)
    └── functions/     ← Edge Functions
        ├── send-new-order/     ← התראה על הזמנה חדשה
        ├── send-order-ready/   ← "מוכנה לאיסוף" למזמין
        └── send-test-email/    ← אבחון הגדרות מייל
```

**פריסה:** הממשק — `git push` מכאן. השרת — `supabase functions deploy <שם>` מכאן.

---

# מדריך הקמה (Supabase)

המערכת עברה מ-Google Sheets + Apps Script ל-**Supabase**. התוצאה: תגובה של 50–200ms במקום 2–5 שניות.

> ⏱️ זמן הקמה משוער: **25–35 דקות**. עברו על השלבים לפי הסדר.

---

## מה השתנה

| יכולת | פירוט |
|-------|-------|
| מסד נתונים | Supabase (PostgreSQL) — מהיר, עם הרשאות ברמת השורה |
| התחברות | Google **או** אימייל+סיסמה (ללא מייל אימות) **או** אורח |
| פרטי משתמש | שם בראשי תיבות + טלפון נשמרים פעם אחת; אורח ממלא בכל הזמנה |
| תעודת ספק | העלאת PDF שמשויך להזמנה, **או** "אגיש באיסוף" |
| מייל "מוכנה לאיסוף" | נשלח אוטומטית באישור, עם הנחיה ליצור קשר עם קצין הדת |
| ארכיון | הזמנות מאושרות עוברות ללשונית "ארכיון" בממשק הניהול |
| ייצוא אקסל | כפתור "⬇ אקסל" בלשונית מלאי |
| לוגו | משולב בכותרת, במסך הכניסה וכאייקון האתר |

---

## שלב 1 — יצירת פרויקט Supabase

1. היכנס ל-**[supabase.com](https://supabase.com)** → **Start your project** → התחבר (אפשר עם Google).
2. **New project**:
   - **Name**: `rabanut-98`
   - **Database Password**: לחץ *Generate* ו**שמור אותה** במקום בטוח.
   - **Region**: `Central EU (Frankfurt)` — הכי קרוב לישראל.
3. המתן ~2 דקות עד שהפרויקט מוכן.

---

## שלב 2 — יצירת הטבלאות

1. בתפריט הצד → **SQL Editor** → **New query**.
2. פתח את הקובץ `supabase/schema.sql`, העתק את **כל** התוכן, הדבק ולחץ **Run**.
3. אמור להופיע `Success. No rows returned`.
4. הרץ את המיגרציות מתוך `supabase/migrations/` (או `supabase db push` דרך ה-CLI).

> 📦 **נתוני המלאי ההתחלתיים** (152 פריטים) יובאו כבר בהקמה הראשונית.
> קובץ הייבוא `seed_inventory.sql` **אינו נמצא בריפו הזה** — הריפו ציבורי,
> ורשימת המלאי נשמרת מקומית בלבד בתיקייה `private/` שמחוץ לריפו.

> ✅ לבדיקה: **Table Editor** → טבלת `inventory` — אמורים להופיע הפריטים.

---

## שלב 3 — חיבור האפליקציה

1. **Project Settings** (גלגל שיניים) → **API**.
2. העתק שני ערכים:
   - **Project URL** — למשל `https://abcdefgh.supabase.co`
   - **anon / public** key — מחרוזת ארוכה
3. פתח את הקובץ `orders/config.js` והדבק:

```js
window.APP_CONFIG = {
  SUPABASE_URL: 'https://abcdefgh.supabase.co',
  SUPABASE_ANON_KEY: 'eyJhbGci...',
  ADMIN_EMAIL: 'rabanoot98@gmail.com',
};
```

> 🔒 מפתח ה-anon בטוח לפרסום — ההגנה היא ב-RLS שהוגדר בשלב 2.

---

## שלב 4 — הגדרות התחברות

**Authentication → Sign In / Providers** (או **Providers**):

### א. אימייל + סיסמה (ללא מייל אימות)
1. **Email** → ודא שהוא **Enabled**.
2. **כבה** את `Confirm email` — כך משתמש נכנס מיד אחרי הרשמה, בלי מייל אימות.

### ב. כניסת אורח
1. חפש **Anonymous sign-ins** → **הפעל**.

### ג. כתובות מורשות
**Authentication → URL Configuration**:
- **Site URL**: `https://rabanoot98.github.io/orders/`
- **Redirect URLs** — הוסף את שתי השורות:
  ```
  https://rabanoot98.github.io/orders/
  http://localhost:8765/
  ```

---

## שלב 5 — התחברות עם Google

יש לך כבר Client ID מהגרסה הקודמת — **אפשר להשתמש בו שוב**:
`690622925527-3q0k73ioffnodq11meu6d4tvbb4aooug.apps.googleusercontent.com`

### א. ב-Google Cloud Console
1. [console.cloud.google.com](https://console.cloud.google.com/) → **APIs & Services → Credentials**.
2. לחץ על ה-OAuth Client הקיים.
3. תחת **Authorized redirect URIs** הוסף (החלף `abcdefgh` במזהה הפרויקט שלך):
   ```
   https://abcdefgh.supabase.co/auth/v1/callback
   ```
4. ודא שתחת **Authorized JavaScript origins** קיימים:
   ```
   https://rabanoot98.github.io
   http://localhost:8765
   ```
5. **Save**. בצד ימין העתק את ה-**Client secret** (אם אינו מוצג — *Reset secret*).

### ב. ב-Supabase
1. **Authentication → Providers → Google** → **Enable**.
2. הדבק **Client ID** ו-**Client Secret** → **Save**.

> ⚠️ אם בהתחברות מופיע *"access blocked / app isn't verified"* — ב-Google Cloud → מסך ההסכמה → **Publish app**, או הוסף את המייל תחת **Test users**.

---

## שלב 6 — מייל "ההזמנה מוכנה לאיסוף"

### א. יצירת סיסמת אפליקציה ל-Gmail
1. [myaccount.google.com/security](https://myaccount.google.com/security) — ודא ש**אימות דו-שלבי מופעל** (חובה).
2. [myaccount.google.com/apppasswords](https://myaccount.google.com/apppasswords) → צור סיסמה בשם `Supabase`.
3. העתק את **16 התווים** (בלי רווחים).

### ב. הגדרת המשתנים ב-Supabase
**Project Settings → Edge Functions → Secrets** (או **Configuration → Secrets**) — הוסף:

| שם | ערך |
|----|-----|
| `GMAIL_USER` | `rabanoot98@gmail.com` |
| `GMAIL_APP_PASSWORD` | 16 התווים משלב א' |
| `RAV_CONTACT` | פרטי קצין הדת, למשל: `קצין הדת — 050-1234567` |

### ג. פרסום הפונקציה
יש **שתי** פונקציות לפרסם:

| שם הפונקציה | מתי נשלח | למי |
|-------------|----------|-----|
| `send-new-order` | כשנכנסת הזמנה חדשה | לכתובות שבלשונית "התראות" |
| `send-order-ready` | כשהמנהל מאשר הזמנה | למזמין |

**האפשרות הקלה — דרך הדפדפן:**
1. תפריט צד → **Edge Functions** → **Deploy a new function** → **Via Editor**.
2. שם: `send-new-order` (בדיוק כך) → הדבק את `supabase/functions/send-new-order/index.ts` → **Deploy**.
3. חזור על התהליך עבור `send-order-ready` עם `supabase/functions/send-order-ready/index.ts`.

**או דרך CLI:**
```bash
npm i -g supabase
supabase login
supabase link --project-ref <PROJECT_REF>
supabase functions deploy send-new-order
supabase functions deploy send-order-ready
```

---

## שלב 7 — הגדרת המנהל

המנהל נקבע **אוטומטית** לפי המייל: מי שנכנס עם `rabanoot98@gmail.com` מקבל הרשאת מנהל.

1. היכנס לאפליקציה והתחבר עם `rabanoot98@gmail.com`.
2. בתפריט ☰ אמור להופיע **🛠️ ניהול**.

> אם לא הופיע: **Table Editor → profiles** → מצא את השורה שלך → שנה `role` ל-`admin` → רענן.

---

## שלב 8 — העלאה לאוויר

```bash
cd "C:\Users\micae\OneDrive\Desktop\אפליקציית דת 98\orders"
git add -A
git commit -m "Migrate to Supabase"
git push
```

תוך 1–2 דקות: **https://rabanoot98.github.io/orders/** (רענן עם `Ctrl+F5`).

---

## בדיקה מקומית

```bash
cd "C:\Users\micae\OneDrive\Desktop\אפליקציית דת 98\orders"
python -m http.server 8765
```
→ http://localhost:8765

> ⚠️ שים לב: מקומית המערכת עובדת מול **הנתונים האמיתיים** ב-Supabase (אין יותר מצב דמה).

---

## פתרון תקלות

| תסמין | סיבה וטיפול |
|-------|-------------|
| "נדרשת הגדרה ראשונית" | `config.js` ריק — שלב 3 |
| כניסת אורח נכשלת | Anonymous sign-ins כבוי — שלב 4ב |
| התחברות Google מחזירה לדף הכניסה | חסר ה-callback ב-Google או ב-Redirect URLs — שלבים 4ג + 5א |
| נרשמתי ולא נכנסתי | `Confirm email` עדיין דלוק — שלב 4א |
| אין כפתור "ניהול" | ה-`role` אינו `admin` — שלב 7 |
| ההזמנה אושרה אך המייל לא נשלח | בדוק Secrets ולוגים ב-Edge Functions → send-order-ready → Logs |
| לא מגיעה התראה על הזמנה חדשה | ודא שהפונקציה `send-new-order` פורסמה, ושיש כתובת **פעילה** בלשונית "התראות" |
| "אין הרשאה" בפעולת ניהול | אתה לא מחובר כמנהל, או ה-RLS חוסם — ודא `role = admin` |

---

## הערות

- **הזמנות היסטוריות** (93 שורות) לא הועברו — הן נשארו בגיליון הישן כארכיון קפוא. המערכת מתחילה נקייה.
- **כפילות באקסל**: `שקי חלל לאימון` הופיע פעמיים בגיליון זו"ק (פעם עם רווח בסוף) — יובא פעם אחת.
- **גיבוי**: כפתור "⬇ אקסל" מייצא את המלאי בכל רגע. מומלץ אחת לחודש.
- **תעודות ספק** נשמרות ב-Storage בדלי `supplier-certs` (פרטי — רק המזמין והמנהל רואים).
